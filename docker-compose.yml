version: '3.9'
services:
  backend:
    build: ./backend
    container_name: fastapi_backend
    ports:
      - "8004:8000"
    env_file:
      - .env

  frontend:
    build: ./frontend
    container_name: react_frontend
    ports:
      - "84:80"
